#cli #messaging #service 

A compilation of [[GCloud CLI]] commands to interact with [[PubSub]] service

# Message

- Publish message to topic
```sh
gcloud pubsub topics publish ${TOPIC_NAME} --message "Hello"
```

